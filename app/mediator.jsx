/*global define, module, require*/
define(['../vendor/lodash/lodash.min',
    '../vendor/minivents'], wrapper);

var wrapper = function(postal) {

    function Mediator() {
	
    }

    return Mediator;
};

